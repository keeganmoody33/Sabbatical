# Sabbatical

A single-subject retrospective study of a 15 month job search, written up to journal standard.

**Start with `AGENTS.md`.** Then `MANIFEST.md` for what every file is and which one wins. Then **`VERIFY.md`**, an adversarial audit of the repo assembly itself, which has already caught one false claim.

## State, 2026-08-30

| | |
|---|---|
| Phase | retrieval, late stage. **Nothing of the paper is drafted** |
| Census | **325 applications.** Stated only in `knowledge/08-census-ledger.md` |
| Codebook | frozen at rev 2, amendments A1 to A4 in force |
| Coder 1 | has not run. Corpus not frozen |
| Retrieval logs | 001 to 051, no 029 |
| Study window | 2025-06-01 to 2026-08-29, America/New_York |

## The one rule

Every number traces to a named artifact, or it is tagged as recall, or it is not written.

## Next actions, in order

**Keegan drafts. Cursor verifies and compiles. Cursor does not draft.** See `paper/README.md`.

**Cursor, now, unblocked:**
1. `VERIFY.md` Part A. Six checks, all inside this repo, no raw files needed. Output to `logs/retrieval-log-052.md`.
2. Fix the `codebook.md` path in `prompts/README-coder.md` to point at `knowledge/03-codebook.md`.

**Cursor, as `raw/` fills:**
3. `VERIFY.md` Part B, file by file. B1 (the fifth Jobright row) and B2 (Gmail miss rate) are the two that change headline numbers.

**Keegan, in parallel:**
4. Draft sections into `paper/`, each ending in a claims block.
5. Settle counting rule 6. See `logs/retrieval-log-051.md`.

**Blocked on a decision, not on work:**
6. Freeze `protocol/CORPUS-MANIFEST.md` and run coder 1. Independent parallel coding needs the raw artifacts, not the log summaries.
