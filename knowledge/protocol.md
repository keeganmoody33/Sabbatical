<!-- kit-meta
file: protocol.md
tier: 0 (durable)
created: 2026-08-29 14:24 ET
updated: 2026-08-30 ET
review-by: 2027-08-29
sources: [S1] [S2] [S6]
-->

# Pre-registration

Frozen before extraction begins. Any change after the first coder runs must be logged here with a date and a reason, and disclosed in the paper.

## Study window

2025-06-01 to 2026-08-29, America/New_York, inclusive. 15 months.

Declared harvest boundary: the prior audits searched only 2025-08-25 forward, so 2025-06-01 to 2025-11-01 is unharvested rather than empty. No time series may be published until that gap is swept.

## Unit of analysis

One application cycle. `company_canonical + role_as_listed + cycle`. Defined in `knowledge/03-codebook.md`.

## Primary and secondary outcomes

- Primary: count of confirmed applications in the `application` register, reported at two strata, `employer_artifact` and full census.
- Secondary: application-to-interview rate, role lane distribution, time to first response, time to first interview.

Precision-constrained metrics (time to response, time to interview) are computed only on rows where both dates carry `precision = exact`. The excluded n is reported alongside.

## Coding design: independent parallel coding

Multiple LLM coders extract the same artifact set using `prompts/extraction.md`, without seeing each other's output. This is the design decision that upgrades the paper from one person's spreadsheet to a measured instrument.

Requirements, in order:

1. **Freeze the codebook first.** No vocabulary changes after coder 1 begins.
2. **Freeze the artifact set.** All coders receive the identical corpus. A coder who sees more artifacts is not a second rating, it is a different study.
3. **Blind.** No coder sees another coder's rows before all runs are complete. If any coder's output is read first, independence is destroyed and the agreement statistic is invalid.
4. **Adjudicate after, not during.** Disagreements are resolved in a named pass, and the pre-adjudication disagreement rate is published.

## Reliability statistics to report

- Percent agreement and Cohen's kappa on `role_lane`, across coders, on the subset both coded.
- Percent agreement on the binary include or exclude decision (`register = application` versus exclusion).
- Disagreement inventory: every row where coders differed, with the field and both values.

`role_lane` is the field that matters most. The current data already shows a 20 point gap in explicit GTM engineering share between the `employer_artifact` stratum (50.9 percent) and the `platform_log` stratum (30.2 percent). Until independent coders agree on lane assignment, that gap cannot be attributed to behavior rather than coding.

## Completeness estimation

Replace the asserted "88 to 93 percent" with an estimate that has a method behind it.

**Method: two-source capture-recapture, stratified.**

Naive Lincoln-Petersen across the full corpus will fail here, and the paper should say why. Gmail ATS receipts and the LinkedIn applied list are not independent captures. LinkedIn Easy Apply frequently generates no ATS mail at all, so the two sources are structurally near-disjoint by construction. Applying the estimator to the raw overlap of 17 against 163 and 99 yields an implausible population and would be a misuse of the method.

The defensible version restricts the estimate to the stratum where both sources could have observed the same application: LinkedIn rows for roles that were submitted through an external ATS rather than Easy Apply. Within that stratum, overlap estimates the unseen, and the resulting interval is extrapolated with the stratum share stated as an assumption.

Report the point estimate, the interval, the independence assumption, and the direction of likely bias. A completeness figure with a method and a caveat is publishable. A completeness figure asserted from feel is not.

**On the 95 percent target:** 95 percent completeness is a goal, not a claim that can be verified without a gold standard. What the paper can defend is a stated interval, the method that produced it, and the list of unmet stop conditions. If the interval's lower bound sits below 95, say so.

## Stop conditions

The census is not closed until all of these are met or explicitly waived in writing:

1. Gmail swept 2025-06-01 to 2026-08-29 for ATS domains, receipt phrases, and Sent mail with attachments.
2. Google Calendar swept for the same window in 90 day blocks without keyword filtering. Keyword search for "interview" previously returned zero events because loops lived in generic invite titles.
3. LinkedIn applied list confirmed complete, including whether pages beyond 10 exist.
4. Ladders applied list exported in full, not only the three Apply4Me receipts.
5. Jobright tracker exported in full.
6. YC Work at a Startup dashboard inspected.
7. Talentpluto and Jobgether underlying employers resolved or formally excluded.

## Known integrity defects to resolve before publication

1. WorkOS, GTM Engineer, 2025-08-25, Tier A, marked Interviewed in the 212 row ledger, absent from the 247 row ledger. Resolve before any interview count is published.
2. The 212 to 163 reconciliation is undocumented.
3. The current dedupe key omits cycle, producing two false duplicate keys.

## Changelog

- 2026-08-29 14:24 ET: created from [S1] [S2] [S6]. Window set to 2025-06-01 through 2026-08-29. Gmail and Google Calendar named as authorized sources by the user on 2026-08-29.
- 2026-08-30 ET: coding correction after coding and adjudication were complete, logged here under the rule at the head of this file. The Weave 2026-08-18 `hiring_manager_interview` was attached to `weave|business-development-manager|c1`, an application already rejected 2025-07-31. The artifact `gth_0339a17e3860d167` is a post-interview decline, so an interview did happen, but for a separate 2026 opening. That opening is inbound with no submission artifact anywhere in the corpus, so it is adjudicated to the opportunity register on the same rule as The Hog. The event is excluded from the interview derivation by named decision in `adjudication/adjudicate.py`, and the Weave application's terminal outcome reverts to `rejected_no_interview` dated 2025-07-31, which is bravo's coding. Consequence: interviewed applications 14 becomes 13, the rate 14/221 = 0.0633 becomes 13/221 = 0.0588, `rejected_after_interview` 6 becomes 5, `rejected_no_interview` 73 becomes 74. The application census stays 221 and the full census stays 298. Time to first interview moves from n=12 median 6 mean 40.3 max 387 to n=11 median 6 mean 8.8 max 34, so that single event was carrying the mean. Source of the correction: the author, from recall, after seeing the analysis. That is the failure mode blind coding exists to prevent and is disclosed as such. Two things support it. Bravo independently excluded the same artifact during blind coding as having no parent, so the correction moves the census toward the blind coder's judgement. And cursor and bravo disagreed on this row's terminal outcome, a field adjudication never covered. The role title, the counterparty, and the inbound origination remain author recall and are not written into any structured field, per `prompts/extraction.md` rule 8. Coder CSVs were not edited. Disclosed in `paper/DEFECTS.md`.
- 2026-08-30 ET: pipeline change after coding and adjudication were complete, logged here under the rule at the head of this file. `adjudication/ingest_platform.py` gained an `ambiguous` match status. The tier 3 matcher already refused to choose when token-prefix equivalence returned more than one Freeze 1 candidate, but the refused row fell through to the net-new branch, where it was indistinguishable from a row with no counterpart. A possible duplicate could therefore enter the full census with nothing recording that it was unresolved. Ambiguous rows now carry `match_status = ambiguous` and a `candidate_parent_ids` column, are written to `adjudication/platform_match.csv`, and are held out of `adjudication/applications__full_census.csv`, on the ground that an omitted row is recoverable and an inflated census is not. Reason for the change: a defect found while auditing the repository, disclosed in `paper/DEFECTS.md`. No published figure moved. Zero rows hit the branch on the current corpus, verified by instrumenting the three tiers before the change: 46 exact, 6 unspecified-fallback, 4 unique-equivalent, 77 with no candidate. `platform_match.csv` remains 134 rows at overlap 56, net-new 77, opportunity or non-census 1. `applications__full_census.csv` remains 298 and byte-identical. Because no real row exercises the new branch, it was tested against synthetic Freeze 1 rows rather than by re-running alone. This change alters the instrument, not the frozen corpus, and no coder output was recoded.

- 2026-08-30 ET: pipeline and reporting layer added after coding and adjudication were complete, logged here under the rule at the head of this file. No stage of the instrument changed and no coder output was recoded. What was added: `pipeline/run.py`, a single entrypoint that runs the existing scripts in dependency order and, in its default check mode, hashes every declared output before and after and exits non-zero if an existing file changed. The full pipeline was verified byte-reproducible against the committed outputs before the entrypoint was written, and the guard subsequently caught a real change during development, which is the behaviour it exists for. Also added: `pipeline/build_views.py` and `views/`, materializing the tables the paper quotes from, asserting census 221, full census 298, interviewed 13 and latency base 196 on every run; `pipeline/data_quality.py` and `data_quality_report.md`, generated rather than written, reading the controlled vocabularies out of the root `codebook.md` at run time so the validator cannot drift from the frozen instrument; `pipeline/origin_taxonomy.csv`, a presentation lookup read by the view layer only and deliberately not shared with the matcher's alias table, because a lookup shared between the census and the analysis would let an analysis change move the census; and `schema.md` and `assumptions.md`, which write down the grain, the keys, the per-metric unit of analysis, and every inference rule with its status. One new defect was found by the added consistency check and is disclosed in `paper/DEFECTS.md`: `jobmail-io|growth-lead|c1` carries an interview event and a stored `terminal_outcome` of `rejected_no_interview`, from one coder with no blind second reading. It is left open rather than resolved by preference. No published figure moved; every previously committed output is byte-identical.

- 2026-08-30 ET: **Freeze 3.** A second, independently produced reconstruction of the same fifteen months arrived after Freeze 1, blind coding and adjudication were complete. It is not a coding input and is not merged: the protocol requires every coder to receive an identical corpus, so a reconstruction built from a different source set is a different study rather than a second rating, and no agreement statistic is computed against it. It is reconciled adversarially in `challenge/CHALLENGE.md`, with redacted extracts under `challenge/` and personal names replaced by `per_<sha256[:12]>` pointers. The workbook itself is not committed, because it names third parties and this repository is public.

  One sheet is primary evidence and is the only thing ingested: the LinkedIn Job Applications data download, 107 rows carrying job IDs, job URLs and exact application dates, now at `artifacts/platform/linkedin_job_applications_export.csv`. It supersedes the 99-row paged scrape, whose relative stamps this protocol forbids upgrading. The scrape is kept because 13 of its companies are absent from the export. Full census date precision moves to 284 exact of 317, with 6 relative stamps left.

  Two register reversals follow from that artifact. The Hog and BX Studio were the only include-or-exclude disagreements between the blind coders, and both were adjudicated to the opportunity register on the single ground that no submission artifact existed. The export carries one for each, dated 2026-06-04 and 2026-04-06, eleven and two days before the process events already coded. The rule is unchanged and its premise is now false, so the rule reverses the outcome by itself. This is what `what_would_promote_it` exists for. Each row is taken from the coder who read it as an application, so no row was assembled during adjudication. Consequence: census 221 becomes 223, interviewed 13 becomes 14, the rate 13/221 becomes 14/223, latency base 196 becomes 197, full census 298 becomes 317.

  Not adopted: the workbook's 353-record count, which mixes both registers; its origin categories on all 353 rows, which are another instrument's unblinded judgments; its GHOSTED and REJECTED outcome model, which its own defect register calls an overloaded residual; and its interview counts, UNKNOWN on 334 of 353. Its 53 LinkedIn recruiter threads are deferred rather than rejected.

  Where this census wins the exchange: 8 of the 10 rows the workbook excludes for unresolvable origin are here with Tier A employer artifacts, and 27 companies here appear nowhere in it. Neither reconstruction is complete and the union is larger than either, which is a further reason no completeness percentage is published.

  Freeze 3.1, same date, prompted by re-reading the challenger for what it got right rather than for where it lost. The challenger resolves role titles to 8 unrecoverable rows where this census carried 40 at `unspecified`. Coding those `unspecified` was correct under counting rule 8, the Gmail receipt does omit the title, but the title was sitting in `artifacts/platform/jobright_applications_log.csv` and `artifacts/platform/linkedin_job_applications_export.csv`, both already committed here. The platform matcher marked the rows `overlap`, kept the Freeze 1 row wholesale, and dropped the title the platform row carried. `adjudication/adjudicate.py` now backfills a title onto a census row coded `unspecified` when exactly one platform title exists for that company under strict normalization, refusing any company with more than one, and sets `evidence_tier = B` because corroboration across two artifacts is what tier B means. Eleven rows qualified, zero were ambiguous, and every backfilled title matches the challenger independently. `application_id` is deliberately NOT regenerated: the events tables join on it and a new slug would orphan every event on the row, so the id keeps its original slug and a new `role_title_source` column records the divergence. Consequence: unspecified role lanes 36 to 25, unspecified titles 40 to 29, `explicit_gtm_engineering` 87 to 92. Census stays 223, full census 317, interviewed 14, latency base 197, and the interview set is unchanged. Audit trail in `adjudication/title_backfill.csv`. The 19 further titles the challenger resolves from job newsletters, LinkedIn job recommendations and archived listings are NOT adopted: each shows the company had an opening by that name in that period, not which opening was applied to.

  Two side effects worth recording. The ambiguous match branch added earlier fires on real rows for the first time, on the Attentive and FOSSA second cycles, which is the case it was written for. And stop condition 3 is now Met for the applied list and still Unmet for the channel label, because the export carries no Easy Apply flag, so capture recapture remains unmeasured. Coder CSVs were not edited and the Gmail and Calendar corpus was not recoded. Disclosed in `paper/DEFECTS.md`.

- 2026-08-30 ET: **supplementary ledger**, replacing the company-grain reconciliation of the challenger with a row-grain one. Prompted by being told to rework the challenge thoroughly. The workbook's `Thread/Dedup Key` is `Company + Role + Date`, 353 values with zero duplicates, which is a record identifier and was available from the start. The first pass matched at company grain instead and called that a deliberate weakening; it was too coarse to adjudicate anything. `challenge/build_supplementary_ledger.py` now writes `challenge/supplementary_ledger.csv`, a full outer join over all 353 workbook records and all 329 repository records (census plus opportunity register), matched through four ordered tiers that refuse rather than guess when a tier returns more than one candidate, with a disposition and a stated reason on every row. It adopts nothing on its own: Freeze 3 and Freeze 3.1 were separate decisions made with artifacts, and the ledger records them as dispositions so they are visible in one place. Headline corrections to the company-grain reading: 257 of 353 workbook records agree outright, records genuinely absent from this corpus are **3** rather than 20 companies, and census records absent from the workbook are **41** rather than 27 companies, 35 of them Tier A. Four records are refused as ambiguous, all of them the Attentive and FOSSA second-cycle pairs, which is now the third independent mechanism to refuse on that same shape. Seven are register disagreements where the workbook counts a process this repository holds as opportunity. Twenty-seven are titles the workbook resolves from evidence outside this corpus and remain unadopted and visible. One matcher defect was found and fixed in `challenge/reconcile.py`: `company_key` could not match a canonical company name under four characters against its own longer form, so Exa Labs read as workbook-only while `exa|growth-lead|c1` sat in the census. `exalabs` is aliased to `exa`, restating a merge `adjudication/ADJUDICATION.md` already recorded, and the alias table is consulted after suffix stripping as well as before. No census figure moved: 223, 317, 14, 197 all unchanged. Documented in `challenge/SUPPLEMENTARY-LEDGER.md`, and `challenge/CHALLENGE.md` now carries a superseded banner pointing at it.

- 2026-08-30 ET: **refined challenger workbook, and four redaction defects found while ingesting it.** A second version of the challenger arrived, `combined_job_search_audit_checkpoint_refined.xlsx`, sha256 `d32c869a...`, holding 343 records against the previous 353. `challenge/extract_checkpoint.py` now accepts either version by hash and names which one it read. Nothing about the census changed: 223, 317, 14, 197 all hold, and `artifacts/platform/linkedin_job_applications_export.csv` is byte-identical, so no ingested artifact moved. What changed is the reconciliation and the redaction.

  Substantively the refined workbook adopts a two-register split of its own, in a new `Interview & Opportunity Register` sheet carrying an `Included in 343 App Ledger?` column. Counting its rows with at least one formal round gives 14 interviews, and this census also derives 14. **The two sets agree on 8.** Six on each side are unique, and the six it holds alone are four register disagreements plus Mixmax, Pinn and Hotglue, which this corpus does not hold at all. That a matching total can hide a six-way disagreement is recorded in `challenge/CHALLENGE.md` as a reliability finding in its own right. No published figure moves, because none of its six unique interviews sits on a census row here.

  Two regressions in the refined workbook are recorded rather than adopted. It removes eight receipts for unresolvable role title, and six of the companies behind them are in this census, five at Tier A, with Hightouch carrying a named role of Go-to-Market Engineer. That is the same failure the challenge already names on origin: a field the schema cannot resolve must not gate inclusion of the row. And it removes four school-district and substitute-teaching rows as false population records, which is a scope judgement rather than an error. Applied consistently here that rule reaches 15 rows, 14 of them Tier A, moving the census 223 to 208 and the rate 14/223 to 14/208. None of the 15 is in the interview set. Left open for the author; no figure assumes either answer.

  **Four redaction defects**, all introduced by this repository and all found by auditing the committed extracts rather than reported by anyone. Every one of them wrote a real person's name into a public repository. First, `looks_like_person` required two to three tokens, so bare given names in the `Contacts / Rounds` column were never rostered: thirteen interviewer first names shipped in the clear. Second, parenthetical content was stripped after splitting rather than before, so a semicolon inside parentheses split "Eddie (2 interviews; final was 1 hour)" into fragments no shape test could match, and `+` was not a separator at all, so "Gurjap Sandhu + Kofi Boamah O." stayed one six-token fragment. Third, the organization guard tested raw substring containment in both directions, and the workbook holds a company named `Vi`, which blocked every name containing those two letters: `Teresa Vitale` and `Vikas CV` shipped because of it. The reverse direction is now length-gated at four characters, the same floor `company_key` uses. Fourth, `UNKNOWN — <person>'s company` placeholders were admitted to the organization set, so `Jacob Bowman` was protected from redaction by a string built out of his own name; placeholders are now excluded.

  The fixes add a distinction the module did not have. Full names are redacted globally, because they are distinctive. Bare given names are redacted only in columns that are not a company, role, title, location or join key, because they are not: `Austin` is an interviewer at Every and also the city in `SDR Manager (Austin; relocation available)` and in two Jobright locations, and redacting it everywhere destroyed a role title to protect nobody. The organization set is now seeded from this repository's own `applications__full_census.csv` as well as the workbook's Company column, because the workbook only lists companies it kept: Lumenalta, Proofpoint and Designit were dropped from its ledger, survive only in a prose note about their removal, and were hashed as people until this census was consulted for what they are.

  A **review gate** now runs on the redacted text before anything is written. Any capitalized word surviving redaction in a person-bearing column that is on no list fails the extraction with the list printed. The single-token prose case cannot be decided by shape, since "Patrick originated the opportunity" and "Community post is the source" are identical in form, so it is decided by review once and recorded in `PROSE_GIVEN_NAMES` and `PROSE_NON_NAMES`. Failing loudly is the point: the previous version made this call silently. The roster moved from 94 names to 107 full names plus 91 scoped given names, and the extracts were audited in both directions, no person in the clear and no company or job title destroyed. Consequence for published figures: none. `challenge/supplementary_ledger.csv` changed because two redacted join keys changed, with every disposition count identical. Coder CSVs were not edited and the corpus was not recoded. Disclosed in `paper/DEFECTS.md`.

- 2026-08-30 ET: **author recall received after the analysis was seen, quarantined not adopted.** The author supplied interview counts, counterparty names and origins for roughly twenty processes. Recorded in `knowledge/04-author-recall.md` with a structured companion at `knowledge/author_recall__2026-08-30.csv`, names as `per_` pointers. **No structured census field was written and no published figure moved: 223, 317, 14, 197 all hold.** `prompts/extraction.md` rule 8 forbids recall in a structured field, and recall arriving after the author has read the results is the most contaminated evidence class this study can take, so it is treated as a retrieval plan rather than as data.

  Two claims were verified before the rest was recorded. The author named a counterparty at Hotglue and one at PhrasIQ; hashed on this repository's own convention they produce `per_8c8479ce1bf3` and `per_3b9d8298f1b5`, which are already sitting verbatim in the redacted challenger register the author has not read. An independently produced file and a memory landing on the same one-way hash is not something recall can fabricate. It does not make the remaining claims evidence, and it does raise the prior on them.

  The substantive finding does not depend on any individual claim. `submission_channel` is populated on 213 of 223 rows and `discovery_source` is `unknown` on 206, which the paper published as a null result. The author's origins explain the shape of that absence: an ATS confirmation records that a form was submitted and nothing about how the opportunity was found, and the two are different questions. Great Question and Pearl are coded `ats_direct` correctly, and on this account both originated in a Slack channel. **Origin is therefore not missing at random, it is unobservable in principle from the artifact class this study is built on, and it is missing hardest where the outcomes concentrate.** Seven processes the author reports reaching interview sit outside the 223 denominator, and not one arrived through a cold ATS application: Glytec and The Kiln by LinkedIn DM, Pinn by community Slack, Mixmax through a school network, Hotglue on YC Work at a Startup, Opsin Security through an intermediary recruiter, Mercor through a platform's internal board. The Kiln is in no census row, no opportunity row and no challenger record, so two independent reconstructions both missed a process that reached a founder interview because both were built from email.

  Four census interviews are disputed, and they subtract rather than add. Hypergen, RevSpring, TestGorilla and `jobmail-io|growth-lead|c1` all derive from an invitation, a screen request or a recruiter intro rather than a record of a completed round. Hypergen is the strongest and is not recall-only: bravo blind-coded it `employer_ack`, cursor coded `hiring_manager_interview`, adjudication took cursor, and the author's recall independently agrees with bravo. That is the same shape as the Weave correction and moves the census toward the blind coder rather than away. If Hypergen alone falls the headline is 13/223; if all four fall it is 10/223. None has been actioned and all four are recorded as open threads in `knowledge/02-current.md`.

  Five retrieval sources are now named that would settle most of this: LinkedIn DM threads, the GTM Cafe Slack `#jobsandopportunities` channel, YC Work at a Startup messages, a Mercor account export and Wellfound. The Slack channel was never in this protocol's source list and on the author's account is the single origin behind at least four interviewing processes, so a study that set out to measure which channels convert never authorized retrieval from the one that appears to convert best. Each would be a protocol amendment and a new freeze, not an edit. Coder CSVs were not edited and the corpus was not recoded.

- 2026-08-30 ET: **five artifacts supplied by the author, recorded and not frozen.** Screenshots of a LinkedIn referral thread introducing the author to two co-founders of The Kiln, an inbound YC Work at a Startup message from Hotglue, a dated calendar booking for an exploratory call, and two LinkedIn profiles of community contacts. **Not committed**, on the same rule as the challenger workbook: they show faces, full names and a third party's email address and this repository is public. Recorded in `knowledge/04-author-recall.md` with what each does and does not establish. No published figure moved.

  Two of the five turn out to add nothing, and that is the useful part. The two community contacts hash to `per_9256fc6cfc1a` and `per_64532c58061b`, both already in the challenger's register as `Informal opportunity conversation` with 0 formal rounds and the explicit note "Do not count as a formal hiring interview". The screenshots confirm the conversations happened, which is what the register already said, so the challenger's classification stands and no interview is added on either side. One gains an exact date of 2025-10-27 where the register carried none. With Hotglue and PhrasIQ this makes **four independent hash confirmations** of author recall against a redacted file the author has not read.

  The other two are stronger and both cut the same way. The Kiln thread establishes the opportunity and a referral origin, dated 4 March with no year visible so it is `evidence_bound` rather than exact, and it does **not** establish an interview: the visible thread ends at a proposal to find time to chat. The Hotglue message establishes a YC Work at a Startup origin and resolves the workbook's "Role title unresolved" to Business Development Manager, and it is inbound outreach, so it does not establish that the author applied. **Neither is an application, so neither would enter the 223 denominator even if adopted in full**, and the same holds for Mixmax, Glytec and Opsin Security. Every process the author has now evidenced reached its outcome through a channel that generates no application receipt, which is the origin finding arriving from the outcome side rather than the field-coverage side.

  One conflation is recorded so it does not enter the analysis. The author's position on RevSpring, TestGorilla and `jobmail-io` is that a "thank you for applying" receipt proves an application and that 500 applications cannot be recalled individually. Both points are correct and neither was in dispute: all three rows stay in the 223 as applications and nothing proposed removing them. The open question is narrower, whether the interview flag on each is real, and each derives from a screen request, a recruiter intro, or a single coder with no blind second reading. The author's own reasoning settles the method: if 500 applications cannot be recalled then recall is the wrong instrument for that flag in both directions, and the three are to be resolved by re-reading artifacts already held. Hypergen remains outside that class, being a specific memory of a distinctive non-event corroborated by a blind coder. Coder CSVs were not edited and the corpus was not recoded.

- 2026-08-30 ET: **recalled discovery layer added at the author's direction.** The author objected that `prompts/extraction.md` rule 8, which forbids recall in a structured field, guarantees `discovery_source` stays empty forever on the one question where recall is often the only witness: a job can be discovered from a friend, a Slack channel, a cohort, or a piece of paper on the ground, and none of those leaves an email receipt. The objection is correct. The design answers it without weakening the census.

  New inputs: `knowledge/discovery_source_recalled.csv`, keyed by `application_id`, and `knowledge/discovery_venues.csv`, a venue-to-venue edge list. New stage in `pipeline/build_views.py` emitting `views/discovery_source.csv` (223 rows) and `views/funnel_by_discovery_source.csv`, the `funnel_by_origin` the original brief asked for.

  Six rules make it safe. The recalled value lives in a **separate file and is never written into `discovery_source`**, because the census is artifact-derived and its byte reproducibility is the study's central claim, so a layer the author revises at will must not sit inside it. **A coded value always beats a recalled one**, so recall fills the residual and can never overwrite a blind coding. Every resolved value carries a `basis` of `coded_artifact`, `author_recall` or `none` and never blank, so any figure is recomputable with recall excluded, and `n_from_author_recall` prints on every funnel row. A **conflict is surfaced, not resolved**: exactly one exists, a Mercor row the author places on the marketplace's internal board where the artifact names a referring person. The **vocabulary is read from `codebook.md` at run time** and extended by exactly four terms, `gtm_cafe_slack`, `gtm_engineering_school`, `linkedin_inbound_dm` and `platform_internal_board`. And **`codebook.md` is not edited**: the blind coders worked against it and adding a term retroactively would change the instrument they used, which `codebook.md` warns against explicitly. The frozen `newsletter_community` was deliberately left unused because it collapses a named channel into a generic bucket and the named channel is the finding.

  The venue edge list is the author's own construction and is the more original part. A job found in a Slack channel was not found in a Slack channel, it was found through whatever led there, and the author reports reaching GTM Cafe through GTM Engineering School. The walk to a root venue is cycle-guarded, so a rollup can ask which root a process traces back to rather than counting the proximate venue.

  **No published figure moved: 223, 317, 14, 197 all hold, and 200 of 223 rows remain `unknown`,** so the origin null result in `assumptions.md` section H stands as written. Eight rows gained a value from recall and fifteen already carried one from an artifact. The result worth recording is that the most interesting cell cannot be published: every application the author can trace to `gtm_cafe_slack` reached an interview, three of three, and n=3 sits below the MIN_CELL floor of 5, so the row prints with its n and a blank rate. The view is built to make that unmissable rather than to smuggle a 3/3 into a chart. Documented in `assumptions.md` section H2 and `views/VIEWS.md`. Coder CSVs were not edited and the corpus was not recoded.

- 2026-08-30 ET: **Freeze 4. An invitation is not a round. Interviewed applications 14 becomes 11 and the headline rate 14/223 becomes 11/223.** The author disputed four of the fourteen from recall. The dispute was resolved from the frozen corpus rather than from the recall, on the author's own reasoning: five hundred applications cannot be recalled individually, so recall is the wrong instrument for this flag in either direction. Every fact below was in the corpus before the author said anything.

  The defect is in the derivation, not in a coder. Interviews are counted by `event_type`, and the codebook gave no rule separating an artifact that **proposes** a conversation from one that **records** one, so `recruiter_screen` and `hiring_manager_interview` were assigned to invitations as readily as to completed rounds. Reading all fourteen threads, a completed interview leaves at least one of five marks and all ten survivors carry one: a scheduling confirmation or calendar reminder naming an interviewer, a decline phrased as after a process, a candidate-experience survey, a `SENT` message referencing the conversation, or a later stage presupposing the first. Hypergen carries an interview invitation dated 2026-04-14 and nothing after it. TestGorilla carries a recruiter intro plus an assessment invitation. RevSpring carries a screen request. None carries any of the five.

  Two things make Hypergen the sharpest case. `artifacts/gmail/retrieval-log-006.md` recorded at capture time that the thread holds an invitation and that the prior ledger's Interviews sheet does not list Hypergen, and nothing acted on that note for the length of the study. And bravo blind-coded the same artifact `employer_ack` while cursor coded it an interview and adjudication took cursor, so the blind coder was right and the adjudication introduced the error rather than catching it. On TestGorilla and RevSpring both coders made the same call, which is why those two are recorded as a missing codebook rule rather than a bad adjudication.

  Applied through `EVENT_EXCLUSIONS` in `adjudication/_common.py`, the same named-exclusion mechanism as the Weave correction, so each event is removed from metrics with a written reason rather than deleted. No terminal outcome is reverted: all three rows are `still_open` with no date, so unlike Weave nothing else moves. `jobmail-io|growth-lead|c1` is **not** removed and stays an open defect, because its decline says the requested steps were completed, which an asynchronous screening would also produce.

  Consequences. Census stays 223 and full census stays 317. Time to first interview moves n=11 median 6 mean 8.8 range 0 to 34, to **n=8 median 5.5 mean 7.1 range 0 to 24**. `explicit_gtm_engineering` moves 10/92 to **7/92**. Titles carrying GTM language move 10 of 14 to **7 of 11**. Interview set found by both coders moves 11 of 14 to **9 of 11**, and bravo's set is now a strict subset of cursor's, which is itself a reliability finding: bravo was systematically the more conservative reader and adjudication systematically preferred the less conservative one. The lane structure does not move, three of eight lanes hold every interview and five lanes covering 83 applications hold none. In the challenge, this census now reports 11 where the workbook reports 14 and the two still agree on the same eight, so the earlier matching totals were a coincidence in which each side's errors cancelled.

  Evidence table for all fourteen in `adjudication/INTERVIEW-EVIDENCE.md`. Disclosed in `paper/DEFECTS.md` and `assumptions.md` section H3. Coder CSVs were not edited and the corpus was not recoded.

- 2026-08-30 ET: **cleartext personal names in the frozen corpus, decided and left in place.** `artifacts/gmail/*.md` carries nine personal names in cleartext across six retrieval logs while the same files redact sender addresses to `eml_` pointers. These are third parties in a public repository. Raised with the author, who elected to leave the corpus as it stands. Recorded in `paper/DEFECTS.md` and `paper/NAMED-COMPANIES.md` so it is a live decision before publication rather than an oversight found afterwards. No file was edited.

- 2026-08-30 ET: **the personal account is reachable. Stop condition closed, and the study has been built on the wrong mailbox for interview scheduling.** The author authorized Gmail and Google Calendar retrieval. `list_calendars` now returns `keeganmoody33@gmail.com`, the calendar `artifacts/calendar/q8-transferred-empty.md` recorded as absent from `list_calendars` and named as an unfiltered-sweep stop-condition miss. That note predicted the interview loops for Beautiful.ai, Pearl, Great Question and Hologram would live there. **The prediction was correct on every company it guessed.** The corpus reached `33@lecturesfrom.com` for Gmail and a transferred calendar holding 33 events of which exactly one was a hiring event, so interview scheduling has been retrieved from the wrong mailbox for the length of the study. Recorded in `artifacts/gmail/retrieval-log-030.md`, redacted to `per_` pointers with no phone numbers, passcodes or conference links.

  Targeted queries only, against named open items. **Nothing is coded and no figure moved: 223, 317, 11, 197 all hold**, because the protocol requires blind independent coding on a frozen corpus and this material has had neither.

  Three results. **Hotglue is confirmed**: a YC Work at a Startup approach on 2026-04-14, then an emailed Google Meet invitation from the counterparty's company address on 2026-04-15 booking a named 30 minute slot on 2026-04-20, then a message from the same counterparty after that slot. A booked slot with a named counterparty is not an offer to book, and the role resolves to Business Development Manager. It stays an opportunity-register row because the approach was inbound and nothing was submitted, so it does not reach the 223 denominator on any reading. **Round counts are undercounted**: Hologram carries two separately scheduled rounds where the census codes one, and Pearl carries two where the census codes one, both matching author recall exactly. The published metric is interviewed applications rather than rounds so no headline moves, but the corpus systematically undercounts rounds. One counterparty pointer from the live calendar, `per_c326cb3fc4f6`, is already present in the challenger workbook's Hologram row, a fifth independent hash confirmation.

  **Opsin Security, Pinn and The Kiln have no email or calendar trace at all.** All three are processes the author reports reaching interview and all three originate in a LinkedIn DM or a Slack channel. An inbound DM leading to a call arranged in the same thread produces no email and no calendar invitation, so these are invisible to email retrieval **by construction**, and fixing the mailbox does not recover them. That is the origin finding arriving from a third direction and it is stronger than the field-coverage version.

  Also recorded: the calendar holds a run of interview events between 2025-02 and 2025-04, before the window opens, for SDR, BDR and account executive roles. None enters the census. They establish that the window cuts across an earlier and differently shaped job search, which bears on how selection is described rather than on any count.

- 2026-08-30 ET: **HartleyCo origin corrected by the author, recorded as a conflict and not adopted. No figure moved.** The author corrected their own earlier naming of the HartleyCo recruiter, confirming the retrieval log's name over their recall, and in the same correction stated that the role came from a friend through GTM Cafe. The census codes `discovery_source = recruiter_inbound`.

  The artifact supports the author. `artifacts/gmail/retrieval-log-018.md` records the recruiter "contacting Keegan **about a GTM Engineer application**", so an application already existed when contact was made and the recruiter was the response rather than the discovery. `recruiter_inbound` was never a reading of that artifact, it was an inference from it, and the artifact's own wording contradicts the inference: the coded value answers "who contacted me" where `discovery_source` asks where the role was found. This is a codebook definition problem rather than a recall-against-artifact dispute.

  It is **not adopted**, because `views/discovery_source.csv` resolves on the rule that a coded value always beats a recalled one, and suspending that rule for a single row would make it meaningless. The conflict flag exists so a case like this is visible rather than silently resolved, and it is now doing exactly that on two rows, this one and the Mercor referral. What adoption would change: `gtm_cafe_slack` from 3 applications and 3 interviews to **4 and 4**, still suppressed at n=4 below the cell floor of 5, and `recruiter_inbound` to one row with **zero** interviews. Under that reading every census interview with a known origin traces to GTM Cafe or Wellfound and none to a recruiter approach. Recorded in `assumptions.md` section H2 as an open adjudication for the author.

  Separately, `jobmail-io|growth-lead|c1` has reopened. It was kept at Freeze 4 as ambiguous because its decline crediting "completing the requested steps through" a named party could describe an asynchronous screening. The author supplied the decline, and the party named is an automated screening product rather than a person at the employer, with the sending address and reply-to both belonging to that product. That is the asynchronous reading, and it is the one that would remove the row, taking 11/223 to 10/223. Not applied: the author explicitly chose to keep this row at Freeze 4 on weaker evidence than now exists, so the reversal is theirs.

- 2026-08-30 ET: **jobmail.io removed and the HartleyCo origin adjudicated. Interviewed applications 11 becomes 10, the rate 11/223 becomes 10/223.** Two author decisions, both supported by artifacts the author supplied rather than by recall.

  `jobmail-io|growth-lead|c1` was kept at Freeze 4 as ambiguous. Its decline credits "completing the requested steps through" a named party, and on the retrieval note alone that could have described a live screen. The author supplied the decline: the named party is an **automated screening product**, not a person at the employer, and both the sending address and the reply-to belong to that product rather than to the hiring company. The steps were asynchronous and machine-administered. Removed through `EVENT_EXCLUSIONS` on the same mechanism as the other three. This also closes the standing defect on that row, a derived interview contradicting a stored `rejected_no_interview` from one coder with no blind second reading: the contradiction is gone because the derived interview is gone.

  **All four Freeze 4 removals were errors this census made and the challenger workbook did not.** On the interview question specifically, the independently produced workbook was the more accurate of the two instruments, and it took a row-grain comparison plus the author's own files to establish that. The two datasets now report 10 and 14 and still agree on the same eight.

  Separately, the **HartleyCo origin is adjudicated to `gtm_cafe_slack`**, overriding the coded `recruiter_inbound`. `views/discovery_source.csv` gains an `overrides_coded` column: the default rule that a coded value beats a recalled one is unchanged and remains the default, and this is a documented escape hatch for the case where the coded value is an inference the artifact does not support. Rows resolved this way carry `basis = author_adjudicated`, distinct from `author_recall`, so any figure can still be recomputed with author input excluded. Exactly one row uses it.

  Consequences. Census stays 223 and full census stays 317. Time to first interview moves n=8 median 5.5 to **n=7 median 5**. `growth_demand_marketing` moves 2/23 to **1/23**. GTM-language titles move 7 of 11 to **7 of 10**. Interview set found by both coders moves 9 of 11 to **9 of 10**, with bravo's set still a strict subset of cursor's. Lane structure is unchanged at three of eight lanes holding every interview and five lanes covering 83 applications holding none.

  The discovery funnel is the result worth recording. `gtm_cafe_slack` is now **4 applications and 4 interviews**, and `recruiter_inbound` is **1 application and 0 interviews**. Of the ten interviews, four trace to that one community channel, one to Wellfound, and five have no known origin. **Every interview in the census with a knowable origin came from GTM Cafe or a job board, and none from a recruiter approach.** The community cell is still suppressed at n=4 below the cell floor of 5, so the rate is not published, which remains the correct treatment and remains the sharpest argument for authorizing retrieval from that channel.

  Origin evidence also supplied and recorded: the Glytec inbound is a LinkedIn DM from the CEO stating the author was personally recommended by his network, and the CEO's name hashes to `per_5864edd8d1eb`, matching the challenger workbook's Glytec origin field exactly, a sixth independent hash confirmation. Mixmax originates in a LinkedIn DM from a GTM Engineering School contact. Opsin Security's first round was scheduled by an intermediary recruiting firm and handed to the company's own executive for the second, confirming the two-round intermediary pattern. None of these three is a census row, so none moves a figure. Coder CSVs were not edited and the corpus was not recoded.
